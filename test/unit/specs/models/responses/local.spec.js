xdescribe('Responses local', () => {

  describe('creating', () => {
    it('should throw an error if no responses are passed', () => {
      
    })
    
    it('should create a response and store it in rxDB', () => {
  
    })
  })
  
  describe('getting', () => {
    it('should return an array of responses if they exist', () => {
      
    })
    
    it('should return an empty array if no responses', () => {
      
    })
  })

})