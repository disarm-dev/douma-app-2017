import '@/models/plan.model.js'

describe('Plan model', () => {
  it('should be created with valid inputs', () => {
    const valid = {
      country: 'bob',
      selected_target_area_ids: [1,2,3]
    }
  })

  it('should not be created with invalid inputs', () => {

  })
})
