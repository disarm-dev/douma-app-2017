xdescribe('plan.vue', () => {

  xdescribe("create new plan", () => {
    xit('can load all available spatial_hierarchies', () => {

    })

    xit('can display selected spatial_heriarchy', () => {})
  })
  xdescribe("view existing plan", () => {})
  xdescribe("edit existing plan", () => {})
  xdescribe("clear existing plan", () => {})



  xit('should get latest plan from remote', () => {})

  xit('should get village-structure-clusters from remote', () => {})

  xit('should calculate the number of days to spray from the inputs given', () => {})

  xit('should calculate the correct risk scale', () => {})

  xit("should set 'unsaved_changes' be true after making a change", () => {})

  xit('should load remote plan when "Cancel edits"', () => {})

  xit("should clear the plan when 'Clear Plan' is clicked", () => {})

  xit("should save the plan when 'Save' is clicked", () => {})

  xit('should show controls when in edit mode', () => {})

  // MAP
  xit('should show areas by risk when selected', () => {
    taregt_ares = [1,2,3,4]
    store = { state: {taregt_ares:}}
  })

  // TABLE
  xit('should display all selected areas in the table', () => {})

})